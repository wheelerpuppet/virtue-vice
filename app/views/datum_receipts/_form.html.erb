<%= form_for(@datum_receipt) do |f| %>
  <% if @datum_receipt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@datum_receipt.errors.count, "error") %> prohibited this datum_receipt from being saved:</h2>

      <ul>
      <% @datum_receipt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :time_stamp %><br>
    <%= f.datetime_select :time_stamp %>
  </div>
  <div class="field">
    <%= f.label :data_id %><br>
    <%= f.number_field :data_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
